spring.application.name=hello-docker

# Reads PORT env var; falls back to 8080 if not set
# In Dockerfile: ENV PORT=8080
server.port=${PORT:8080}

# Expose /actuator/health for container health checks and Kubernetes liveness probes
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
